sql {
    dialect = "postgresql"
    driver = "rlm_sql_postgresql"
    postgresql {
        send_application_name = yes
    }
    radius_db = "host={{database.host}} port={{database.port}} dbname={{database.name}} user={{database.user}} password={{radius_db_password}} sslmode=verify-full sslrootcert=system sslcert={{conf_root}}/certs/server.crt sslkey={{conf_root}}/certs/server.key"

    acct_table1 = "radacct"
    acct_table2 = "radacct"
    postauth_table = "radpostauth"
    authcheck_table = "radcheck"
    groupcheck_table = "radgroupcheck"
    authreply_table = "radreply"
    groupreply_table = "radgroupreply"
    usergroup_table = "radusergroup"
    client_table = "nas"

    pool {
        start = ${thread[pool].start_servers}
        min = ${thread[pool].min_spare_servers}
        max = ${thread[pool].max_servers}
        spare = ${thread[pool].max_spare_servers}
        uses = 0
        retry_delay = 30
        lifetime = 0
        idle_timeout = 60
    }
    group_attribute = "SQL-Group"

    # Read database-specific queries
    $INCLUDE ${modconfdir}/${.:name}/main/${dialect}/queries.conf
}
